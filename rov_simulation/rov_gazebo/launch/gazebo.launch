<launch>
  <!-- We resume the logic in empty_world.launch, changing only the name of the world to be launched -->
  <include file="$(find-pkg-share gazebo_ros)/launch/gazebo.launch.py">
    <!-- more default parameters can be changed here -->
  </include>

<param name="robot_description" command="$(find-pkg-share xacro)/xacro $(find-pkg-share rov_description)/robots/rov.xacro" />

  <!-- Spawn a robot into Gazebo -->
  <!--
<node name="spawn_urdf" pkg="gazebo_ros" type="spawn_model" args="-param robot_description -urdf -model rov" />
-->
</launch>